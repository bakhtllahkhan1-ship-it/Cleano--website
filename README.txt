# Cleano Website
Static starter website for GitHub Pages.

Important:
- Keep `index.html` in the root of the repository.
- Keep `styles.css` and `script.js` beside `index.html`.
- This version contains a demo booking form only; real bookings, WhatsApp, payments, database, cleaner dashboard and admin panel can be connected later.
